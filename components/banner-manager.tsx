"use client";

import { ChangeEvent, DragEvent, FormEvent, useState } from "react";
import { ArrowIcon, ImageIcon, PlusIcon, TrashIcon } from "@/components/icons";
import { ConfirmDialog, ToastStack, type ToastMessage } from "@/components/feedback";
import { ProductVisual } from "@/components/product-visual";
import type { HeroSlide } from "@/lib/types";

async function toWebP(file: File) {
  const bitmap = await createImageBitmap(file); const width = Math.min(2000, bitmap.width); const scale = width / bitmap.width;
  const canvas = document.createElement("canvas"); canvas.width = Math.round(bitmap.width * scale); canvas.height = Math.round(bitmap.height * scale);
  const context = canvas.getContext("2d"); if (!context) throw new Error("Unable to process this image."); context.drawImage(bitmap, 0, 0, canvas.width, canvas.height); bitmap.close();
  const blob = await new Promise<Blob | null>((resolve) => canvas.toBlob(resolve, "image/webp", .86)); if (!blob) throw new Error("Image conversion failed.");
  return new File([blob], "banner.webp", { type: "image/webp" });
}

export function BannerManager({ initialSlides }: { initialSlides: HeroSlide[] }) {
  const [slides, setSlides] = useState(initialSlides); const [title, setTitle] = useState(""); const [subtitle, setSubtitle] = useState(""); const [ctaLabel, setCtaLabel] = useState("Shop now"); const [ctaHref, setCtaHref] = useState("/#products"); const [image, setImage] = useState<File | null>(null); const [preview, setPreview] = useState(""); const [status, setStatus] = useState(""); const [saving, setSaving] = useState(false); const [dragging, setDragging] = useState(false); const [deleteTarget, setDeleteTarget] = useState<HeroSlide | null>(null); const [deleting, setDeleting] = useState(false); const [toasts, setToasts] = useState<ToastMessage[]>([]);
  function notify(message: string, tone: ToastMessage["tone"] = "info") { setToasts((items) => [...items, { id: Date.now() + Math.random(), message, tone }]); }
  async function processImage(selected: File) { setStatus("Optimizing banner…"); try { const optimized = await toWebP(selected); if (preview) URL.revokeObjectURL(preview); setImage(optimized); setPreview(URL.createObjectURL(optimized)); setStatus("Banner ready to publish."); } catch (error) { setStatus(error instanceof Error ? error.message : "Unable to process image."); } }
  function pickImage(event: ChangeEvent<HTMLInputElement>) { const selected = event.target.files?.[0]; if (selected) void processImage(selected); }
  function dropImage(event: DragEvent<HTMLLabelElement>) { event.preventDefault(); setDragging(false); const selected = event.dataTransfer.files?.[0]; if (selected) void processImage(selected); }
  async function createSlide(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!image) return setStatus("Choose a wide banner image first."); setSaving(true); setStatus("Publishing banner…"); const form = new FormData(); form.set("title", title); form.set("subtitle", subtitle); form.set("cta_label", ctaLabel); form.set("cta_href", ctaHref); form.set("sort_order", String(slides.length)); form.set("image", image); try { const response = await fetch("/api/slides", { method: "POST", body: form }); const result = await response.json(); if (!response.ok) throw new Error(result.error ?? "Unable to publish banner."); setSlides((items) => [...items, result]); setTitle(""); setSubtitle(""); setCtaLabel("Shop now"); setCtaHref("/#products"); setImage(null); if (preview) URL.revokeObjectURL(preview); setPreview(""); setStatus(""); notify("Banner published.", "success"); } catch (error) { notify(error instanceof Error ? error.message : "Unable to publish banner.", "error"); setStatus(""); } finally { setSaving(false); } }
  async function patchSlide(slide: HeroSlide, updates: Partial<HeroSlide>) { try { const response = await fetch(`/api/slides/${slide.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(updates) }); const result = await response.json(); if (!response.ok) throw new Error(result.error ?? "Unable to update banner."); setSlides((items) => items.map((item) => item.id === slide.id ? result : item)); notify("Banner updated.", "success"); } catch (error) { notify(error instanceof Error ? error.message : "Unable to update banner.", "error"); } }
  async function duplicateSlide(slide: HeroSlide) { try { const response = await fetch(`/api/slides/${slide.id}`, { method: "POST" }); const result = await response.json(); if (!response.ok) throw new Error(result.error ?? "Unable to duplicate banner."); setSlides((items) => [...items, result]); notify("Banner duplicated.", "success"); } catch (error) { notify(error instanceof Error ? error.message : "Unable to duplicate banner.", "error"); } }
  async function removeSlide(slide: HeroSlide) { setDeleting(true); try { const response = await fetch(`/api/slides/${slide.id}`, { method: "DELETE" }); if (!response.ok) { const result = await response.json().catch(() => ({ error: "Unable to delete banner." })); throw new Error(result.error); } setSlides((items) => items.filter((item) => item.id !== slide.id)); setDeleteTarget(null); notify("Banner deleted.", "success"); } catch (error) { notify(error instanceof Error ? error.message : "Unable to delete banner.", "error"); } finally { setDeleting(false); } }
  return <section className="banner-manager"><div className="panel-heading"><div><p className="eyebrow">Homepage editor</p><h2>Hero slideshow</h2></div><span>Up to 5 slides</span></div><div className="banner-manager-layout"><form className="banner-form" onSubmit={createSlide}><label className="field"><span>Headline</span><input required maxLength={100} value={title} onChange={(event) => setTitle(event.target.value)} placeholder="A clear campaign headline" /></label><label className="field"><span>Supporting text</span><textarea maxLength={240} rows={3} value={subtitle} onChange={(event) => setSubtitle(event.target.value)} placeholder="A short reason to explore this feature" /></label><div className="banner-fields"><label className="field"><span>Button label</span><input required maxLength={40} value={ctaLabel} onChange={(event) => setCtaLabel(event.target.value)} /></label><label className="field"><span>Button link</span><input required value={ctaHref} onChange={(event) => setCtaHref(event.target.value)} /></label></div><label className={`banner-upload ${dragging ? "is-dragging" : ""}`} onDragEnter={(event) => { event.preventDefault(); setDragging(true); }} onDragOver={(event) => event.preventDefault()} onDragLeave={() => setDragging(false)} onDrop={dropImage}><input type="file" accept="image/*" onChange={pickImage} />{preview ? <ProductVisual src={preview} alt="Banner preview" /> : <><ImageIcon width={27} height={27} style={{ width: 27, height: 27, maxWidth: 27, maxHeight: 27 }} /><strong>Drop or choose a wide image</strong><span>Recommended 2000 × 1100 or wider</span></>}</label>{status && <p className="form-status" role="status">{status}</p>}<button className="admin-submit" type="submit" disabled={saving || slides.length >= 5}>{saving ? "Publishing…" : <><PlusIcon /> Publish banner</>}</button></form><div className="banner-list">{slides.length ? [...slides].sort((a, b) => a.sort_order - b.sort_order).map((slide) => <article className={`banner-list-item ${slide.active ? "" : "is-disabled"}`} key={slide.id}><div className="banner-list-image"><ProductVisual src={slide.image_url} alt="" /></div><div><small>Slide {slide.sort_order + 1}</small><h3>{slide.title}</h3><p>{slide.subtitle}</p><div className="banner-actions"><button type="button" onClick={() => void patchSlide(slide, { active: !slide.active })}>{slide.active ? "Hide" : "Show"}</button><button type="button" onClick={() => void duplicateSlide(slide)}>Duplicate</button><a href={slide.cta_href} target="_blank">Preview <ArrowIcon /></a><button type="button" className="danger-action" onClick={() => setDeleteTarget(slide)}><TrashIcon /></button></div></div></article>) : <div className="admin-empty"><ImageIcon /><h3>No custom banners yet</h3><p>The storefront is using its designed fallback hero.</p></div>}</div></div><ToastStack toasts={toasts} dismiss={(id) => setToasts((items) => items.filter((item) => item.id !== id))} />{deleteTarget && <ConfirmDialog title={`Delete ${deleteTarget.title}?`} message="The banner record and image will be removed from Supabase." busy={deleting} onCancel={() => setDeleteTarget(null)} onConfirm={() => void removeSlide(deleteTarget)} />}</section>;
}
